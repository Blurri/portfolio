/**
 * Settings Collections
 *
 * This group contains collections that manage global site settings,
 * user accounts, and configuration options.
 */

import { Settings } from './settings'
import { SocialLinks } from './social-links'
import { Users } from './users'

export const SettingsCollections = [Settings, SocialLinks, Users]
