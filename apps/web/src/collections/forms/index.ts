/**
 * Forms Collections
 *
 * This group contains collections that store user-submitted form data
 * rather than content managed by admins.
 */

import { Contact } from './contact'

export const FormsCollections = [Contact]
